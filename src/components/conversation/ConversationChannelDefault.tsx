import { StyledConversationChannel } from "../../styled/conversation";

const ConversationChannelDefault = () => {
  return (
    <StyledConversationChannel>Select a channel</StyledConversationChannel>
  );
};

export default ConversationChannelDefault;
